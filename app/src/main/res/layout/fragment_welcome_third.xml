<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:background="@color/white"
    tools:context=".Fragment.WelcomeThirdFragment">

    <ImageView
        android:id="@+id/first"
        android:layout_width="match_parent"
        android:layout_height="450dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="-130dp"
        android:scaleType="fitXY"
        android:src="@drawable/m5" />

    <ImageView
        android:id="@+id/inner"
        android:layout_width="130dp"
        android:layout_height="130dp"
        android:layout_below="@id/first"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="-65dp"
        android:background="@drawable/welcome_back_items"
        android:src="@drawable/kdf" />

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/inner"
        android:layout_marginStart="@dimen/dp_20"
        android:layout_marginTop="@dimen/dp_10"
        android:text="Lorem lpsum"
        android:textColor="@color/light_yellow"
        android:textSize="@dimen/sp_18"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/name"
        android:layout_marginStart="20dp"
        android:layout_marginTop="@dimen/dp_8"
        android:layout_marginEnd="@dimen/dp_20"
        android:layout_marginBottom="@dimen/dp_10"
        android:maxLines="2"
        android:text="Lorem lpsum is simply dummy text. Lorem lpsum is simply dummy text."
        android:textColor="@color/light_yellow" />


</RelativeLayout>